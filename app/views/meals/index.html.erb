<div class="main meals-index">
  <div class="container">
    <h1 class="users-heading">all meals</h1>
    <% @meals.each do |meal|%>
      <div class="meals-index-item">
        <%#byebug%>
        <%= link_to(meal.title,"/meals/#{meal.id}")%>
        <div class="meal-user-name">
          <%= link_to(meal.user.name,"/users/#{meal.user.id}")%>
          <%#= meal.title %>
          <%#= meal.user.name %>
        </div>
      </div>
    <% end%>
  </div>
</div>