<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name%></h2>
      <p><%= @user.email%></p>
      <% if @user.id==@current_user.id%>
        <%= link_to("modify", "/users/#{@user.id}/edit") %>
      <%end%>
    </div>
    <% @user.meals.each do |meal| %>
      <div class="meals-index-item">
        <%= link_to(meal.title, "/meals/#{meal.id}") %>
      </div>
    <% end %>
  </div>
</div>